import 'package:flutter/material.dart';

class Registeredexpenses extends StatelessWidget {
  const Registeredexpenses({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
